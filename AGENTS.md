# Changelog management
Always add created commits to the CHANGELOG.mdi by commit ID reference with a one-two liner summary and a Github reference to the user.
The section called ### Commits contains these changes by release.

# Git handling: 
- Always sign commits with -s flag
